// Firebase Cloud Messaging is no longer used. This file is kept as an empty stub
// so that any previously cached service worker registrations do not cause 404 errors.
self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', () => self.clients.claim());
